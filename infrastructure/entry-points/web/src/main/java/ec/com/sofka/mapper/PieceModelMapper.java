package ec.com.sofka.mapper;
/*
import ec.com.sofka.data.PieceRequestDTO;
import ec.com.sofka.data.PieceResponseDTO;
import ec.com.sofka.commands.PieceCommand;
import ec.com.sofka.queries.responses.PieceResponse;

public class PieceModelMapper {

    public static PieceCommand mapToRequest(PieceRequestDTO piece) {
        if (piece == null) {
            return null;
        }

        return new PieceCommand(
                piece.getColor(),
                piece.getPosition(),
                piece.isKing()
        );
    }

    public static PieceResponseDTO mapToDTO(PieceResponse piece) {
        if (piece == null) {
            return null;
        }

        return new PieceResponseDTO(
                piece.getColor(),
                piece.getPosition(),
                piece.isKing()
        );
    }
}

 */