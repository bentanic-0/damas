package ec.com.sofka.mapper;
/*
import ec.com.sofka.data.MoveRequestDTO;
import ec.com.sofka.data.MoveResponseDTO;
import ec.com.sofka.commands.MoveCommand;
import ec.com.sofka.queries.responses.MoveResponse;

public class MoveModelMapper {

    public static MoveCommand mapToRequest(MoveRequestDTO move) {
        if (move == null) {
            return null;
        }

        return new MoveCommand(
                move.getPlayerId(),
                move.getPieceId(),
                move.getStartPosition(),
                move.getEndPosition(),
                move.isCapture()
        );
    }

    public static MoveResponseDTO mapToDTO(MoveResponse move) {
        if (move == null) {
            return null;
        }

        return new MoveResponseDTO(
                move.getPlayerId(),
                move.getPieceId(),
                move.getStartPosition(),
                move.getEndPosition(),
                move.isCapture()
        );
    }
}

 */