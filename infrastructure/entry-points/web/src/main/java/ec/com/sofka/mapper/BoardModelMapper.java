package ec.com.sofka.mapper;
/*
import ec.com.sofka.data.BoardRequestDTO;
import ec.com.sofka.data.BoardResponseDTO;
import ec.com.sofka.commands.BoardCommand;
import ec.com.sofka.queries.responses.BoardResponse;

import java.util.Map;

public class BoardModelMapper {

    public static BoardCommand mapToRequest(BoardRequestDTO board) {
        if (board == null) {
            return null;
        }

        return new BoardCommand(
                board.getSize(),
                board.getPieces() // Assuming pieces are directly mappable
        );
    }

    public static BoardResponseDTO mapToDTO(BoardResponse board) {
        if (board == null) {
            return null;
        }

        return new BoardResponseDTO(
                board.getSize(),
                board.getPieces() // Assuming pieces are directly mappable
        );
    }
}

 */